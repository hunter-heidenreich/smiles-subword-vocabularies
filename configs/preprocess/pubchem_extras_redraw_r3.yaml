# Training-subsample spot-check, redraw r3. Independent ~5M draw paired with
# r1 / r2 to bound subsample noise on the (V=512, PubChem, NMB) contrast triple.
#
# Source: the headline `canon_dedup_v1/train` (test-set-disjoint by
# construction). Distinct `hash_domain` per redraw → independent.
name: pubchem
input_dir: data/processed/pubchem/canon_dedup_v1/train
output_dir: data/processed/pubchem/canon_dedup_v1_extras/redraw_r3
target_n: 5000000
hash_domain: canon_dedup_v1.redraw.r3
